org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# --- Release signing (fill these with your real values and keep the keystore out of VCS) ---
RELEASE_STORE_FILE=keystore/release.keystore
RELEASE_STORE_PASSWORD=CHANGE_ME_STORE_PASSWORD
RELEASE_KEY_ALIAS=vinor_release
RELEASE_KEY_PASSWORD=CHANGE_ME_KEY_PASSWORD

